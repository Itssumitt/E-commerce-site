CREATE TABLE USERS
(
	EMAIL VARCHAR(30) PRIMARY KEY,
	UPASS VARCHAR(30),
	UTYPE VARCHAR(10)
);
INSERT INTO USERS VALUES('ADMIN@ADMIN.COM','ADMIN123','ADMINISTRATOR');
CREATE TABLE MEMBERS
(
	EMAIL VARCHAR(30)  PRIMARY KEY,
	MNAME VARCHAR(30),
	MOBILE VARCHAR(10),
	ADDRESS VARCHAR(100),
	STATE VARCHAR(20),
	CITY VARCHAR(20),
	PINCODE VARCHAR(20)
	
);
